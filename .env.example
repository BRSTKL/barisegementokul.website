VITE_LLM_PROVIDER=gemini

VITE_GEMINI_PROXY_URL=/api/interpretive-summary
VITE_GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
VITE_GEMINI_MODEL=gemini-3-flash-preview

VITE_OPENAI_BASE_URL=https://api.openai.com/v1
VITE_OPENAI_MODEL=gpt-5-mini

# Server-only secret. Put this in .env.local or your host environment, not in client-side Vite vars.
GEMINI_API_KEY=
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_MODEL=gemini-3-flash-preview

# Do not put production API keys in Vite client env vars.
# Use the secure backend proxy above for deployment.
